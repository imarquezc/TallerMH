<%= form_for(@work) do |f| %>
  <% if @work.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
      <% @work.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :motivo %><br>
    <%= f.text_area :motivo %>
  </div>
  <div class="field">
    <%= f.label :tipo %><br>
    <%= f.select :tipo, ['Mantención', 'Consulta', 'Otro'] %>
  </div>
  <div class="field">
    <%= f.label :kilometraje %><br>
    <%= f.text_field :kilometraje %>
  </div>
  <div class="field">
    <%= f.label :estanque %><br>
    <div id="estanque"></div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <div class="field">
    <%= f.hidden_field :car, :value => @car %><br>
  </div>
<% end %>


<script type="text/javascript">
    $("#estanque").roundSlider({
        radius: 90,
        circleShape: "half-top",
        sliderType: "min-range",
        showTooltip: true,
        value: 0,
        max:12,
        width:20,
        change: function (e) {
            var value = e.value, color;
            if (value < 3) color = "red";
            else if (value < 6) color = "yellow";
            else color = "green";

            $(".rs-range-color").css("background-color", color) ;
        }

    });
    $(".rs-range-color").css("background-color", "white") ;

</script>
